【需要记忆】下面把你第二天代码里**所有 import 和注解**按文件整理出来，每个都标注功能。以后写代码直接查这张表。

---

## 一、`User.java`

### import
| 导入 | 功能 |
|---|---|
| `com.baomidou.mybatisplus.annotation.IdType` | 主键生成策略枚举 |
| `com.baomidou.mybatisplus.annotation.TableId` | 标记主键字段 |
| `com.baomidou.mybatisplus.annotation.TableName` | 标记类对应哪张表 |
| `lombok.Data` | Lombok 生成 getter/setter/toString/equals/hashCode |

### 注解
| 注解 | 功能 |
|---|---|
| `@Data` | 编译期生成 getter、setter、toString、equals、hashCode |
| `@TableName("user")` | 类 ↔ 表名映射，告诉 MyBatis-Plus 操作 `user` 表 |
| `@TableId(type = IdType.AUTO)` | 标记主键，`AUTO` 表示数据库自增，插入时忽略传入 id，并回填生成的主键 |

【可复制但必须懂原理】
- `@Data` 是 Lombok 编译期注解，不是运行时反射。
- `@TableName`、`@TableId` 是 MyBatis-Plus 元数据，启动时被反射解析成 `TableInfo`。

【面试必考】
- Lombok 原理？`@Data` 包含哪些注解？
- MyBatis-Plus 主键策略有哪些？`IdType.AUTO` 和 `ASSIGN_ID` 区别？

---

## 二、`UserMapper.java`

### import
| 导入 | 功能 |
|---|---|
| `com.baomidou.mybatisplus.core.mapper.BaseMapper` | 通用 CRUD Mapper 接口，提供 insert/selectById/updateById/deleteById 等方法 |
| `org.example.entity.User` | 实体类 |
| `org.apache.ibatis.annotations.Mapper` | 标记这是 MyBatis Mapper 接口，启动时生成动态代理实现 |

### 注解
| 注解 | 功能 |
|---|---|
| `@Mapper` | 告诉 MyBatis 扫描并生成代理实现类 |

【可复制但必须懂原理】
- `BaseMapper<User>` 的泛型 `User` 告诉框架操作哪个实体。
- MyBatis 用 JDK 动态代理为接口生成实现，方法调用被拦截后动态拼 SQL。

【面试必考】
- MyBatis 为什么只用接口就能操作数据库？
- 动态代理在 MyBatis 中的作用？

---

## 三、`UserService.java`

### import
| 导入 | 功能 |
|---|---|
| `com.baomidou.mybatisplus.extension.service.IService` | 通用 Service 接口，定义 save/getById/updateById/removeById/list 等方法 |
| `org.example.entity.User` | 实体类 |

### 注解
无。

【可复制但必须懂原理】
- `IService<User>` 是 MyBatis-Plus 对 `BaseMapper` 的业务层封装，方法更丰富（如 `saveBatch`）。

【面试必考】
- `IService` 和 `BaseMapper` 的区别？
- 为什么 Service 层要再封装一次 Mapper？

---

## 四、`UserServiceImpl.java`

### import
| 导入 | 功能 |
|---|---|
| `com.baomidou.mybatisplus.extension.service.impl.ServiceImpl` | `IService` 的通用实现，继承它自动获得所有方法 |
| `org.example.entity.User` | 实体类 |
| `org.example.mapper.UserMapper` | Mapper 接口，`ServiceImpl` 底层需要它 |
| `org.example.service.UserService` | 业务接口 |
| `org.springframework.stereotype.Service` | 标记这是 Spring 业务层组件，放入 IoC 容器 |

### 注解
| 注解 | 功能 |
|---|---|
| `@Service` | 告诉 Spring 这是业务层 Bean，启动时实例化并放入 IoC 容器 |

【必须自己实现】
- 自己敲一遍 `UserService` 和 `UserServiceImpl`，理解继承链。

【面试必考】
- `ServiceImpl<UserMapper, User>` 两个泛型参数含义？
- 为什么 Service 要分成接口和实现类？

---

## 五、`UserController.java`

### import
| 导入 | 功能 |
|---|---|
| `lombok.RequiredArgsConstructor` | Lombok 为 `final` 字段生成构造方法 |
| `org.example.entity.User` | 实体类 |
| `org.example.service.UserService` | 业务接口，被注入进来调用 |
| `org.springframework.web.bind.annotation.*` | Spring MVC 所有 Web 注解 |
| `java.util.List` | 返回列表时用 |
| `com.baomidou.mybatisplus.core.conditions.query.QueryWrapper`（搜索接口用） | 条件构造器，动态拼 WHERE 条件 |

### 注解
| 注解 | 功能 |
|---|---|
| `@RestController` | 标记控制器，返回值直接作为 JSON 响应（= `@Controller` + `@ResponseBody`） |
| `@RequestMapping("/user")` | 类级别路径前缀，所有方法都在 `/user` 下 |
| `@RequiredArgsConstructor` | Lombok 生成包含 `final` 字段的构造方法，Spring 用它注入 |
| `@PostMapping` | 处理 POST 请求，新增 |
| `@GetMapping("/{id}")` | 处理 GET 请求，按 id 查询 |
| `@GetMapping` | 处理 GET 请求，查询全部 |
| `@PutMapping` | 处理 PUT 请求，更新 |
| `@DeleteMapping("/{id}")` | 处理 DELETE 请求，删除 |
| `@RequestBody` | 把请求体 JSON 反序列化成 Java 对象 |
| `@PathVariable` | 从 URL 路径取参数，如 `/user/1` 中的 `1` |
| `@RequestParam`（搜索接口用） | 从查询参数取参数，如 `/user/search?name=张` 中的 `name` |

【可复制但必须懂原理】
- 注解可以复制，但要能解释：请求怎么路由到方法、参数怎么绑定、JSON 怎么转换。

【面试必考】
- `@RestController` 和 `@Controller` 区别？
- `@RequestBody` 和 `@RequestParam` 区别？
- `@PathVariable` 和 `@RequestParam` 区别？
- 一个 HTTP 请求在 Spring Boot 中的完整处理流程？

---

## 六、汇总：所有 import 和注解速查

### import 汇总
```java
// MyBatis-Plus 注解
import com.baomidou.mybatisplus.annotation.IdType;
import com.baomidou.mybatisplus.annotation.TableId;
import com.baomidou.mybatisplus.annotation.TableName;

// MyBatis-Plus 核心
import com.baomidou.mybatisplus.core.mapper.BaseMapper;
import com.baomidou.mybatisplus.extension.service.IService;
import com.baomidou.mybatisplus.extension.service.impl.ServiceImpl;
import com.baomidou.mybatisplus.core.conditions.query.QueryWrapper;

// MyBatis
import org.apache.ibatis.annotations.Mapper;

// Lombok
import lombok.Data;
import lombok.RequiredArgsConstructor;

// Spring
import org.springframework.stereotype.Service;
import org.springframework.web.bind.annotation.*;
import java.util.List;
```

### 注解汇总
```java
// 实体类
@Data
@TableName("user")
@TableId(type = IdType.AUTO)

// Mapper
@Mapper

// Service
@Service

// Controller
@RestController
@RequestMapping("/user")
@RequiredArgsConstructor
@PostMapping
@GetMapping("/{id}")
@GetMapping
@PutMapping
@DeleteMapping("/{id}")
@RequestBody
@PathVariable
@RequestParam
```

---

## 七、面试必考总结

【面试必考】
- Lombok 原理、`@Data` 包含哪些注解
- MyBatis-Plus 主键策略、`@TableName` / `@TableId` 映射
- MyBatis 动态代理、为什么 Mapper 不用写实现
- `IService` 和 `BaseMapper` 区别
- `ServiceImpl` 泛型参数含义
- 构造方法注入 vs 字段注入
- `@RestController` vs `@Controller`
- `@RequestBody` vs `@RequestParam` vs `@PathVariable`
- 一个 HTTP 请求在 Spring Boot 中的完整流程

【需要记忆】
- 上面所有 import 和注解的用途
- 常用注解清单
- Knife4j 访问路径 `http://localhost:8080/doc.html`

【需要系统补习】
- IoC / DI、反射、动态代理
- MyBatis-Plus 元数据解析、条件构造器
- Spring MVC 请求处理流程
- HTTP 协议、RESTful 风格

---

这张表存下来，写代码时随时查。现在去写搜索接口，用 `QueryWrapper` 或 `LambdaQueryWrapper`，加 `@RequestParam`。写完截图发我。